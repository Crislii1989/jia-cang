# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android signing secrets (do not commit)
/android/key.properties
/android/app/*.jks
/android/app/keystore.properties

# Environment variables (contains secrets, do not commit)
.env
.env.local
.env.*.local

# WorkBuddy agent working data (memory, verify screenshots)
.workbuddy/

# IDE/tool working dirs
.codegraph/
.reasonix/
.trae/

# Agent / 调试临时输出（历次会话遗留的 build*、t*、an*、commit*、scan 等日志，一律不入库）
# ⚠️ 只锚定根目录（/*.txt），**别改成裸 `*.txt`**：
#    assets/prompts/item_recognition_prompt.txt 是 PromptService 实际加载的资产，
#    linux|windows 下的 CMakeLists.txt 是构建文件，它们都是真实文件。
/*.txt

# Agent / 调试临时输出（第二批，2026-09-17）：E2E 驱动/冒烟日志 JSON 与一次性 python 脚本。
# 同样**只锚定根目录**（`/*.xxx`），别改成裸 `*.json` / `*.py`：
#   真实的 .json 全在子目录 —— web/manifest.json、ios|macos 的
#   Runner/Assets.xcassets/**/Contents.json、.vscode/*.json；
#   Flutter 项目根本就不该有 .json / .py（可复用的脚本请放 tool/）。
# 这一批误入库的是 24 个 drive*.json + shot6.json + deadcode.json + 4 个 .py，已清理。
/*.json
/*.py

# drift Web Worker 的编译中间产物。
# drift_worker.js 运行时必需、drift_worker.js.map 被它自己的 sourceMappingURL 引用，两者保留；
# 而这个 .deps 是 dart compile 的依赖清单 —— 仓库里没有任何东西引用它，且里面写满了
# 绝对路径（pub cache 与本机工程路径），属纯噪音，不入库。
web/drift_worker.js.deps
